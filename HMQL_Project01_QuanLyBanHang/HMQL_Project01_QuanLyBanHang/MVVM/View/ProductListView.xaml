<UserControl x:Class="HMQL_Project01_QuanLyBanHang.MVVM.View.ProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodel="clr-namespace:HMQL_Project01_QuanLyBanHang.MVVM.ViewModel"
             xmlns:local="clr-namespace:HMQL_Project01_QuanLyBanHang.MVVM.View"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800" Loaded="Window_Loaded">
    <!--<Window.Resources>
        <local:RelativeToAbsoluteConverter x:Key="absoluteConverter" />
    </Window.Resources>-->
    <UserControl.DataContext>
        <viewmodel:ProductListViewModel />
    </UserControl.DataContext>
    <Grid>
        <Border Background="#FFF1F4FA"
                CornerRadius="24" />
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Popular Now"
                        FontSize="24" FontWeight="Bold"
                        Margin="32 16 10 0" Width="149"
                        HorizontalAlignment="Left" />
                    <TextBlock Text="See more >>"
                        FontSize="18" FontStyle="Italic"
                        Foreground="Gray"
                        Margin="0 16 32 0" Width="149"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Bottom" />
                </StackPanel>

                <Border Height="230"
                    Background="White"
                    CornerRadius="12"
                    BorderThickness="1"
                    BorderBrush="LightGray"
                    Margin="16">
                    <ListView ScrollViewer.VerticalScrollBarVisibility="Disabled" Margin="15" Name="bookList">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical" Width="200" Margin="0 0 10 10">
                                    <Image RenderOptions.BitmapScalingMode="HighQuality" Margin="0, 0, 5, 0" Width="100" Height="100" Source="{Binding Cover}" />
                                    <TextBlock TextWrapping="Wrap" TextAlignment="Center" Margin="0 10 0 0">
                            <Run Text="{Binding Name}" Foreground="Red" />
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                    </ListView>
                </Border>

                <TextBlock Text="New Series"
                       FontSize="22" FontWeight="Bold"
                       Margin="32 16 32 0" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>